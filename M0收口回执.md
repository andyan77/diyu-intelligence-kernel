# M0 收口回执 v3.0（M0 Closeout Receipt · 块 E 收尾后）

## 〇、回执口径（两行，块 E ⑤ / E-14 关闭面）

1. **绑收口树**：本回执每一版只绑定其声明的收口时点树；历史版本的数字对其历史树成立，**不追改、不失效**（v2.0 绑 `7010bf6` 的 80/47/28 项数字对该树为真；与现况的差异是考卷面此后合法演进的结果，不是回执失真）。
2. **持续证明 = CI 必过**：本回执之后任何树的六步全绿由 main 分支 required check `check_m0`（服务端钉版环境）承担——回执不逐批重出；「某个后续提交是否仍绿」查该提交的 CI 回执，不查本文件。

## 〇.1 当前版本绑定（v3.0）

| 项 | 值 |
|---|---|
| **受审树 / audited tree** | commit `53786e265eeed02757c373462507a65bce1d2113`（origin/main，块 E PR #15 squash 合并本体） |
| 取证方式 | **干净 clone**：`git clone --depth 1` 自 GitHub 远程 → `python3 tools/check_m0.py`（六步一键，零手工干预） |
| 六步实测 | [1/6] GATE_GREEN 十三条红线全清 · [2/6] 变异 **58 项全过**（29 缺陷面 + Z-12 运行态横扫 28 条）· [3/6] 检测器 **137/137** · [4/6] BD-D01 三夹具如期 · [5/6] 烟测 4/4 · [6/6] 事实层回归 **210/210**（examples 81 + 正式快照 15×2 + facts 池 49×2 + 正式面计数）→ CHECK_M0_GREEN，HEAD=`53786e2…` 门自读 `.git` 自证 |
| 附加取证（同树干净 clone） | `kernel/facts/test_facts_offline.py` 42/42 GREEN · `kernel/intent/test_intent_offline.py` 47/47 GREEN · `tools/migrate_snapshots.py verify --old-ref 4fdf258` 991/991 PASS |
| 取证环境 | Python 3.10.12；jsonschema 3.2.0；PyYAML 5.4.1 ≠ 钉版 6.0.2（如实报备：本地取证机漂移；钉版环境同树证据由服务端 CI 承担） |
| 服务端 CI（钉版环境） | `53786e2` 本体 check_m0 = **completed / success**（actions/runs/32122124746；GitHub API 实查回执） |
| 取证时间 | 2026-08-18（北京时间；块 E 收尾窗口） |
| 入库口径 | 本 v3 以 **doc-only 后继提交**入库（改动面仅本文件与《裁决台账.md》登记行），§四 永久规矩同款 |

---

# 历史版本 · M0 收口回执 v2.0（绑 `7010bf6`，原文保留不追改）

| 项 | 值 |
|---|---|
| 版本 | v2.0（取代绑定 `24e24a3` 的 v1 版；v1 全文见 git 历史——外部审查判「回执未绑定最终签字树」属实，块 A 裁定表 Z-01） |
| **受审树 / audited tree** | commit `7010bf600b9b1c1f38d277dae9b7bde54f73b923`（origin/main，块 A PR #5 squash 合并本体） |
| 取证方式 | **干净 clone**：全新空目录 `git clone` 自 GitHub 远程 → 直接 `python3 tools/check_m0.py`（六步一键，零手工干预） |
| 取证环境 | Python 3.10.12；jsonschema 3.2.0；PyYAML **5.4.1 ≠ 钉版 6.0.2**（如实报备：本地取证机漂移，check_m0 自打 ⚠ 不影响判绿；钉版环境的同树证据由下方服务端 CI 承担） |
| 服务端 CI（钉版环境） | `7010bf6` 本体 check_m0 = **completed / success**（actions/runs/32102336524；GitHub API 实查回执） |
| 取证时间 | 2026-08-18（北京时间；块 A 收尾，执行窗口留档） |
| 本回执自身的入库口径 | 回执描述的是 `7010bf6` 的树内容；回执+台账恢复行以 **doc-only 后继提交**入库（改动面仅本文件与《裁决台账.md》，零资产/零门代码/零考卷变动）——与 v1 时代「重签提交改 37 个资产文件」的缺陷路径有本质区别，且执行规格 §四 永久规矩已把该口径固化 |

## 一、check_m0 六步实测（干净 clone @7010bf6 原样输出摘录）

```text
[1/6] PASS 冻结断言门 送签态 | GATE_GREEN、exit 0（十三条红线全清）
[2/6] PASS 冻结门负向变异回归 | 28 项全过、真仓零改动隔离 PASS、exit 0
[3/6] PASS 检测器负向测试 | 合计 80 项：通过 80、失败 0、exit 0
[4/6] PASS BD-D01 三夹具回归 | good=PENDING_HUMAN / collapse=FAIL[BD_CANDIDATE_COLLAPSE] / fabricated=FAIL[BD_FACT_FABRICATION]
[5/6] PASS Schema 四例烟测 | 4/4 如期
[6/6] PASS M1 事实层 Schema 回归 | 合计 47 项：通过 47、失败 0、exit 0
CHECK_M0_GREEN | 六步全绿 | HEAD=7010bf600b9b1c1f38d277dae9b7bde54f73b923
```

HEAD 归属块由门自行读 `.git` 输出（未执行 git 命令），受审树与运行树同一性自证。

## 二、块 A 修复后的证据链闭合声明

| 外部审查缺口（裁定表编号） | 闭合方式 |
|---|---|
| Z-01 回执未绑定最终树 | 本回执绑定 `7010bf6` = 全部修复合并后的 main 本体；此后签字动作按 §四 只许改签字字段 |
| Z-02 「改考题保签字」假绿 | R6 内容绑定（`case_manifests` 20 条指纹实时比对）+ M23 活体证据 |
| Z-03 R5 只看键 / 规则不冻结 | R5 值校验 + `rules` 节 digest 冻结 + M21/M22 活体证据 |
| Z-04 冻结历史被覆盖 | 七份旧 digest 补回 version_history + 签字态翻转显式标记例外条款 + M26 边界证据 |
| Z-05 R5/R6/R7 无活体证据 | 变异套件 22→28 项，R1-R13 十三条全部持有负向活体证据 |
| Z-07 coverage 过期 | 已按当前树重生成（RUN 三份重跑挂起：触发点=本回执后、正式运行前，见台账挂起表） |
| Z-08 版本声明漂移 | M0-EP01 v1.1（E v1.1、过期段删除） |

范围外挂起项（C.2 服务端复刻 / CI 依赖封闭化 / RUN 重跑 / 运行态变异覆盖 / 主模型级联）均已登记台账挂起表并带触发点，不在本回执的证据闭合承诺范围内——**本回执只声明其声明的东西**。

## 三、Founder 签字区

- 本回执经 Founder 确认后，《裁决台账》M0 收口状态由「暂缓」恢复「成立」（恢复行与本回执同一 doc-only 提交落盘）。
- 确认记录：Founder（Faye）2026-08-18 经 AskUserQuestion 选择题确认（见台账 08-18「M0 收口恢复」行）。
