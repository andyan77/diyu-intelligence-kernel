# M1 修复窗 · 交接清单（一页；窗口退役件）

> 窗口范围：2026-08-18 修复窗（块 0 → 块 A → 主模型级联/补签 → 块 B → 块 C → 块 E → IA-1 送签前置收尾告别块）。
> 本清单只声明**本窗口车道**的收口状态；其他车道（M1-EP02 Intent 取证 / M2-EP01 提前段）各有其台账行与归属窗口。

## 一、在建资产：零

| 检查面 | 实测状态 |
|---|---|
| 在途 PR | 0（#4/#5/#6/#8/#11-#17 全部合并；自测探针 #22 已关闭未合并、分支已删） |
| 远端遗留分支 | 本车道 4 条（blockC-sign / blockE-receipt / blockE-sign / m1-blockE-vertical）已删；余 main + 本告别块分支（合并后删） |
| 半成品文件 / 草稿 | 无（git status 干净树取证；施工 worktree 均在会话级 scratchpad，不在仓库内） |

## 二、本窗口未决事项：零

| 曾经的未决 | 关闭凭据（均在《裁决台账》08-18 行） |
|---|---|
| 块 C 签字 | 16:04:02 批次（PR #14） |
| 块 E 迁移方案批准 + 三边界令 | Founder 批复原文入台账；对账 991/991 机器件 |
| 块 E 签字 | 17:41:56 批次（PR #17） |
| M0 收口状态 | 「成立」（回执 v3.0 绑 53786e2，两行口径固化） |
| C.2 服务端复刻 + CI 封闭化 | 本告别块（自测 REJECT 实证 run 32134810327） |

## 三、后续触发点：已全部在台账挂起表（本窗口零私藏）

| 事项 | 触发点 |
|---|---|
| Fact 更新触发新 Snapshot（A 行 421，运行时写路径） | 运行时写路径落地（IA-1 后） |
| C.6.1 尝试账本门服务端复刻（本告别块「不新增检查种类」未做） | 未单列挂起——已在 ci.yml 头注与 .githooks/pre-commit 注双处如实披露；若需强制力升级由 Founder 派发 |
| ubuntu-latest 运行器镜像层钉死（OS 级复现） | 若证据要求 OS 级复现，另立一件事走裁决（requirements-lock.txt 头注留痕） |
| 其余（judge 校准集 / OQ-BUILD-05 / BD-D02/03 夹具可执行化 / numeric_grounding 误报提案等） | 属其他车道/Founder 裁决队列，台账挂起表逐行带触发点，非本窗口私藏 |

## 四、留给下一窗口的三把钥匙

1. **门禁唯一入口**：`python3 tools/check_m0.py`（六步；第六步 210 项含正式快照与 facts 池全量）。CI = 同一条命令 + C.2 隔离门前置步（PR 逐 commit），required check `check_m0`。
2. **改考卷的合法路径**：快照/池/规则/Manifest 均有冻结锚（R4/R6/fact_fixtures/rules 节）——改值必升版必同步登记，门不替任何一方拍板；签字批次照 §四 永久规矩（先回执后签字、签字提交只动签字字段）。
3. **依赖再生成**：改 requirements.txt 必同批重生成 requirements-lock.txt（方法在其头注），CI `--require-hashes` 会把漂移当场变红。

窗口退役声明：以上三节实测于本告别块 PR 合并树；此后任何树的持续证明 = main required check `check_m0` 必过（《M0收口回执.md》v3.0 两行口径）。
