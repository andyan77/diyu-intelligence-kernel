# M0 收口修复批次 · 执行规格

> 性质：修复批次唯一施工蓝图（doc）。依据两份外部审查（基线 main@2e516df 判 BLOCK、补充审查基线 main@72be502 维持 BLOCK）+ 执行侧逐条实测核验。
> 状态：**✅ 批次完成（2026-08-18）**——P0-1~P0-6 全部执行完毕；干净 clone 回执《M0收口回执.md》@24e24a3 五步全绿 + CI 双 success；Founder 重签 2026-08-18T02:39:10+08:00 生效；DoD 达成，M0 收口，M1 GO。
> 铁律：修复批次完成、干净 clone 回执出具、Founder 重签之前——**M1 一切实质工作禁止**（含 kernel 实现、Intent 模块、IA-1 取证、把 PENDING_BUILD 换成未裁决版本、**以及用"先做空心骨架"绕过本禁令**）。当前唯一合法施工范围 = 本批次。

## 〇、审查判定"不需修"的项（防过度修复，原样保留）

| 项 | 审查结论 |
|---|---|
| BD-D01 三带案例（good→PENDING_HUMAN / 候选坍缩→FAIL / 库存篡改→FAIL） | 正确，PASS |
| 默认运行态红线基线（P0-2 加固后为 **110 条** = R2×80「PENDING_BUILD 四字段」+ R11×10「PENDING_RESIGN 占位」+ R12×20「撤回签字待重签」；P0-6 重签后预期回落 80 条纯 R2） | 设计内行为，非阻断；check_m0 按类别逐条对照，数不符或新类别即 ⚠ |
| 覆盖率 3/58、case.yaml 1/14、其余 55 条标 human_required | 诚实基线，非阻断 |
| 14 案例 / 20 运行 Manifest 结构关系 | 成立，PASS |
| run_case.py 判分语义（L1 只判 FAIL，无命中→PENDING_HUMAN） | 正确 |

## 一、六项阻断现状（补充审查基线 72be502；当前 HEAD 已前进）

| # | 阻断 | 状态 |
|---|---|---|
| BLOCK-01 | 冻结靠首行覆盖未真收敛 | ⛔ 待修（P0-1） |
| BLOCK-02 | 冻结门可实测假绿（4 类变异 GATE_GREEN） | ⛔ 待修（P0-2） |
| BLOCK-03 | 数字溯源三类漏检 + 运行证据过期（词表 13→16 未重跑） | ⛔ 待修（P0-3/P0-4） |
| BLOCK-04 | OD-05 未裁决 | ✅ **已关闭**（04b5adb：轻量自建，审查基线晚一个提交未见） |
| BLOCK-05 | 无 CI/分支保护/依赖锁/可传播钩子 | ⛔ 待修（P0-5） |
| BLOCK-06 | 云盘历史链接 + 素材授权边界 | ✅ **按 Founder 裁决关闭**（工作树已脱敏 72be502；云盘文件由 Founder 自行删除，不阻塞批次、不列入重签确认项——台账 08-17 特别说明行） |

执行侧精度修正（实测 04b5adb）：审查称 Schema 缺 B.2.1 四必填字段，实测 `model_version` 已由 required 的 `model_name`（承载 qwen-max-0107）+ `model_provider` 覆盖；**真缺三项**：`e2e_interaction_contract_version` / `e2e_output_contract_version` / `baseline_prompt_versions`（含两个嵌套子字段）——20 份 Manifest 实例均有值，但 Schema 允许 null 且不 required，删除不报错，假绿成立。

## 二、修复规格（六步，顺序执行）

### P0-1 真冻结收敛（合同单义化）
- 六份 interaction 文件文档控制块写 `v1.0 / CONTENT-FROZEN（内容定稿）`；**生效标记统一用 `PENDING_RESIGN_P0-6` 占位**，P0-6 Founder 重签后一次性替换为 EFFECTIVE+签字时间（在重签前写 EFFECTIVE = 重蹈"先宣告后签字"）。该 token 白名单位置：六份控制块状态行、anonymity §8 P-07 行、IA-0 签字包控制行+签字区、M0-EP01 状态行、OD-02 控制行、generation_parameters.json 状态字段——白名单外出现即为缺陷。删除活动正文全部 `v0.1-draft / PENDING_IA0 / 未冻结 / 不得使用`（实测 176 处逐一关闭，不许再用首行覆盖声明；PENDING_HUMAN 是运行态判分状态非占位符，保留）
- 匿名流程 P-01~P-14 每项填入具体裁决值或精确裁决引用（责任人 / 两阶段赋值表复用 / 时间窗口 / 违规标签与后果 / 揭盲禁回改 / 防篡改证明 / 问卷归属 / `<thinking>` 剥离边界）
- 19/20、14/20 口径全文统一为 20
- 20 份 Manifest 去 `.draft.yaml` 命名 + 去 DRAFT 头注释，转正式冻结资产名
- OD-02 从 DRAFT 转正式定格记录；IA-0 签字包重写为最终单义状态（去"现在还不用签"等矛盾句）

### P0-2 Schema 与冻结门加固
Schema 新增 required：`e2e_interaction_contract_version` / `e2e_output_contract_version` / `baseline_prompt_versions`（及 decision_stage / creative_stage 子字段；E2E 案例不得为 null）。
冻结门新增校验：
- `generation_parameters.json` 实时重算 SHA-256 与 Manifest 指纹比对（不再只查非空）
- 交互合同 / 输出合同 / Prompt 声明版本实时解析文件真源（杜绝 v999.0 假绿）
- PRD/A/B 正文 digest 校验（不升版偷改正文 → RED，不再只看版本行）
- 活动合同区 `PENDING_IA0` 扫描 → RED
- Case ID / 运行身份全局唯一性
- 30 需求编号 × 14 案例映射完整性
已实测的假绿变异**全部固化为自动回归测试**：删必填字段 / 参数文件漂移 / 合同版本不存在 / 匿名流程未裁完 / 正文无升版漂移 / 重复案例身份 → 必须全 RED。

### P0-3 numeric_grounding v0.2
弃"全快照数字集合 + 阈值 50"算法：数字绑定字段路径与事实对象；带单位核验（件/元/周/天/%/码数）；支持中文数字（一千二百件/六周/八百件）;禁跨商品、日期、版本、尺码借位；价格/库存/周期不设阈值豁免；三类漏检各建负向测试。

### P0-4 证据按 HEAD 重跑
用当前考卷版本（16 词词表等）重生成 RUN-0001/0002/0003、coverage report、参数指纹；所有回执记 commit SHA + 依赖版本 + Python 版本 + 执行命令。

### P0-5 可传播门禁
最小依赖锁；统一入口 `check_m0`（一键跑 Schema+冻结门+案例正负样例+变异回归）；CI 自动执行并设 main required check + 分支保护；钩子安装脚本或关键约束进 CI；全新目录 clone 后一键跑通。

### P0-6 安全与重签
- ~~云盘匿名分享关闭~~ → Founder 08-17 裁决：不阻塞、不列确认项，云盘文件由 Founder 自行删除（台账 08-17 特别说明行）
- ~~OD-05~~ ✅ 已裁决（轻量自建，04b5adb）
- 干净 clone 全跑 → 出具绑定最终 HEAD SHA 的 M0 closeout receipt
- **Founder 基于修复后最终资产重新签字**（不沿用 2026-08-17T19:08:38 旧签字时间）

## 三、完成定义（DoD）

```text
六项 BLOCK 全关闭 + 负向变异回归全 RED-when-should-RED
+ 干净 clone 一键跑通出绑定 SHA 回执
+ Founder 重签
→ M0 判定翻转 BLOCK→PASS，M1 由 NO_GO 翻转 GO
```
