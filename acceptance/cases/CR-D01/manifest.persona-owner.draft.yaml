# 【2026-08-17 预裁决】execution_mode/task_statement 类待裁项已由 Founder 按推荐整体裁决（台账 + OPEN_QUESTIONS 文首）；文中旧「待 IA-0 裁决」字样就此关闭，剩余 PENDING_* 仅为待建设/待定格的值。
# DRAFT——PENDING 字段于 IA-0 冻结时补齐并经 Founder 签字生效
# 案例真源：B.4.3/CR-D01｜同一方向下的人设反事实（B:514-544，只引用不复述）
# 字段定义真源：B.2.1（B:127-165，含 B:165「不得在查看结果后修改事实、裁判问题、允许答案、禁止结果或通过条件」）
#   + contracts/schemas/case_manifest.schema.json
# 运行拆分：B.2.1（B:129）「每个正式案例运行前必须冻结一份 Case Manifest」；B:520-523 要求在其余条件相同下切换两种人设各跑一次
#   → 本案例共 2 份运行 Manifest（manifest.persona-owner.draft.yaml / manifest.persona-buyer.draft.yaml），缺任一份不得计为齐套（是否照此签两份由 IA-0 确认：OQ-CR-D01-03）。
# 本份 = 品牌主理人人设一侧（B:522）；两份除人设输入外全部字段一致，含同一 context_snapshot_id 与同一 snapshot_hash。

case_id: CR-D01
# 与 acceptance/cases/BD-D01/case.yaml 的 v0.1 对齐；草案态由文件名 .draft.yaml、本文件头 DRAFT 注释与 approved_by 表达，不进版本号
case_version: "v0.1"

prd_version: "v0.1"
data_contract_version: "v0.2"                 # A v0.2（Founder 08-17 预裁决⑦补情绪字段）；由 v0.1 机械同步，非新裁决
acceptance_contract_version: "v0.3"

# 规划名：快照尚未构建（本案例目录下无 fixtures/；对照 acceptance/cases/BD-D01/fixtures/context_snapshot.json 才是已落盘实体）。（08-17 已失效：快照已落盘 acceptance/cases/CR-D01/fixtures/context_snapshot.json，snapshot_hash 已回填）
# 两份变体共用同一快照与同一 hash（B:520「使用相同商品、商业目标和已选候选」）；hash 于 IA-0 构建快照后回填（OQ-CR-D01-06）
context_snapshot_id: "SNAP-CRD01-0001"
snapshot_hash: "sha256:77a6a85da43582ec1bb52a969cdf7d8fd877fa73debf1205cebf3f909dfc5671"  # 由 tools/freeze_gate.py 唯一算法实算回填（快照正文任何改动即过期，须同批重算）

# 逐字引自 B:520 与本次运行的人设分支 B:522（含列表符号，未改字）；另一分支 B:523 见 manifest.persona-buyer.draft.yaml。
# B 把两种人设写在同一句「分别切换」指令下，逐运行任务陈述的最终措辞由 IA-0 定（OQ-CR-D01-02）
task_statement: |-
  使用相同商品、商业目标和已选候选，分别切换：

  - 品牌主理人人设；

# B.4.3/CR-D01 未声明 execution_mode；本值为占位（无 B 依据），IA-0 由 Founder 裁决（见 OPEN_QUESTIONS OQ-CR-D01-01）
execution_mode: QUICK  # Founder 2026-08-17 预裁决（台账；原则「考追问行为才开增强模式」）

# B.4.3/CR-D01 无「冻结事实」小节，未给任何规则编号；contracts/rules/ 当前为空目录
# （全仓唯一落盘的规则对象是 acceptance/cases/BD-D01/fixtures/context_snapshot.json 的 R-BDD01-001/002）
# → 空数组，不凭空断言 object_id/version；IA-0 注册规则对象后回填（OQ-CR-D01-04）
hard_rule_refs: []

# contracts/schemas/ 下不存在任何 Creative 输出 Schema（现有 intent_execution_plan / business_decision_bundle / case_manifest / common.defs），
# 且本文件 module_contract_versions.creative 仍为 PENDING_BUILD → 不写看似已存在的版本号（OQ-CR-D01-05）
output_schema_version: "v1.0"  # 真源=contracts/interaction/e2e_output_contract.md 阶段C 九部分 Schema（IA-0 冻结批次）；机器 Schema 文件随 M3 实现落 contracts/schemas/，属实现工件不另立口径

# B.1.4（B:95）：首轮验收关闭 Brand Memory；schema allOf（真源 B.2.2 B:179）：DISABLED 时引用列表必须为空
brand_memory_state: "DISABLED"
approved_brand_memory_refs: []

# 诊断案例（B.4），非 B.5 端到端匿名 A/B → 按 B.2.1（B:146-150）的 string|null 取 null（不适用）
e2e_interaction_contract_version: null
e2e_output_contract_version: null
baseline_prompt_versions:
  decision_stage: null
  creative_stage: null

diyu_build_version: "PENDING_BUILD"
module_contract_versions:
  intent: "PENDING_BUILD"
  business_decision: "PENDING_BUILD"
  creative: "PENDING_BUILD"

# OD-02 厂商组合已裁决（裁决台账 08-17：Qwen + DeepSeek 组合，多模态用 Qwen 系列），版本/参数于 IA-0 定格——占位指后者。
# model_version 与同组三字段同取 PENDING_OD02_IA0（原 null 会被「冻结前 grep PENDING 扫未填项」漏掉）
model_provider: "Alibaba Cloud DashScope"  # Founder 2026-08-17 IA-0 定格（实测两家 models 接口后裁决；参数真源 contracts/interaction/generation_parameters.json）
model_name: "qwen-max-0107"  # Founder 2026-08-17 IA-0 定格（实测两家 models 接口后裁决；参数真源 contracts/interaction/generation_parameters.json）
model_version: "snapshot-0107（models 接口回执 2026-08-17）"  # Founder 2026-08-17 IA-0 定格（实测两家 models 接口后裁决；参数真源 contracts/interaction/generation_parameters.json）
generation_parameters_hash: "sha256:24b85c93d027274afddbb347faf50533822566aa2b0f14365f4a62c4e2759262"  # Founder 2026-08-17 IA-0 定格（实测两家 models 接口后裁决；参数真源 contracts/interaction/generation_parameters.json）

# B.2.2（B:178）只禁「任一侧使用未声明的搜索或知识库」，并未规定 allowed_tools 必须为空；
# B.4.3/CR-D01 未明文允许任何工具 → 草案取空数组作保守占位，待 IA-0 确认（OQ-CR-D01-07）
allowed_tools: []

approved_by: "Founder（Faye）"  # IA-0 签字（2026-08-17 AskUserQuestion 三连裁决，台账登记）
approved_at: "2026-08-17T19:08:38+08:00"
