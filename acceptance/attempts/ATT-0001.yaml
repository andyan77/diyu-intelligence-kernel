# 尝试账本条目（C.6 格式；一条一文件，"无新条目"由 C.6.1 钩子按新增文件的提交时序机器判定）
attempt_id: ATT-0001
hypothesis: "初建 kernel/intent 七类资产（C.3：1 步 LLM + 确定性前后处理）——bootstrap 条目：模块从无到有，非既有 prompt 的调优"
target_failing_run: null   # bootstrap 例外：模块尚不存在，无既往失败 run 可指；C:174「没有失败 run 不许改 prompt」约束的是调优，初建以本条目登记。此后任何 prompt/编排改动必须指向真实失败 run，不得再用 null
regression_after: null     # 建成后由 INT-D01/D02/D03 离线回归与首批 RUN 回执填充（后续条目引用具体 RUN-ID）
delta: {fixed: [], broken: [], net: 0}   # C.6.2 结构化建设第一步 net=0 属正常（豁免死循环信号统计的先例口径）
