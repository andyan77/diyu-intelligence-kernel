# 运行输入补充：fixtures/task_input.json 冻结 stated_business_goal=INVENTORY_ACTIVATION（Founder 08-17 IA-0 裁决，A.4.1 承载，OQ-BUILD-02 已决）
# 【2026-08-17 预裁决】execution_mode/task_statement 类待裁项已由 Founder 按推荐整体裁决（台账 + OPEN_QUESTIONS 文首）；文中旧「待 IA-0 裁决」字样就此关闭，剩余 PENDING_* 仅为待建设/待定格的值。
# DRAFT——本文件是草案：PENDING_* 占位字段于 IA-0 冻结时补齐并经 Founder 签字生效；approved_by 未签字前不得视为已冻结。
# 校验说明：tools/validate_schema.py 只校结构，对 PENDING_* 占位完全无感（且 approved_at 在 B.2.1（B:162）是 datetime，
#   本仓 case_manifest.schema.json 只约束 string、无 format）——SCHEMA_OK ≠ 可冻结（三案例同问，OQ-INT-D01-06）。
# 案例真源：B.4.1/INT-D02（B:305-347）——只引用不复述
# 字段定义真源：B.2.1 Case Manifest（B:127-165）+ contracts/schemas/case_manifest.schema.json
# 运行份数：本案例共 2 份运行 Manifest（manifest.quick.draft.yaml + manifest.enhanced.draft.yaml），缺任一份不得计为齐套。
#   依据：B:129「每个正式案例运行前必须冻结一份 Case Manifest：」；B:317「快速模式：」与 B:325「增强模式：」两族允许答案；
#   B:341-342 人工裁判问题 1「快速模式能否继续但不掩盖不确定性？」与 2「增强模式是否只询问高价值信息？」须两次运行才答得了；
#   B:347 主要失败标签含 INT_MODE_IGNORED。
# 本份 = 快速模式侧（execution_mode: QUICK）；声明的切换项只有 execution_mode，其余字段与 manifest.enhanced.draft.yaml 逐字一致。
#   注：「两份除 execution_mode 外其余字段必须一致（含共用同一 Context Snapshot）」是本草案起草口径，B 未就本案例规定——
#   B.2.2「同条件」（B:167-181）自述管的是正式 A/B 两侧（笛语 vs 基线），不管同一诊断案例的双模式两次运行 → OQ-INT-D02-01。
case_id: INT-D02
case_version: "v0.1"                          # 与 acceptance/cases/BD-D01/case.yaml 的 case_version: v0.1 对齐；草案态由文件名 .draft.yaml + 本文件头 DRAFT 注释 + approved_by 占位表达，不进版本号
prd_version: "v0.1"                           # M0-EP01 文档版本一致性声明：主 PRD v0.1 EFFECTIVE
data_contract_version: "v0.2"                 # A v0.2（Founder 08-17 预裁决⑦补情绪字段）；由 v0.1 机械同步，非新裁决
acceptance_contract_version: "v0.3"           # M0-EP01：附录 B 三个核心模块智能验收合同 v0.3 EFFECTIVE
context_snapshot_id: "SNAP-INTD02-0001"       # 规划名，快照尚未构建（仓库内无对应 fixtures）；两份 Manifest 共用同一快照 id 属草案起草口径，B 未就本案例规定 → OQ-INT-D02-01（08-17 已失效：快照已落盘 acceptance/cases/INT-D02/fixtures/context_snapshot.json）
snapshot_hash: "sha256:2f038bc21405df3072d9ecbe5333055c1e778fb139698e7b4c1cbe569a9a0c68"  # 由 tools/freeze_gate.py 唯一算法实算回填（快照正文任何改动即过期，须同批重算）
task_statement: "为这件羊绒大衣制作春节前视频号内容。"   # 照抄 B:311「输入」引文，一字不改
execution_mode: QUICK                         # 本份的声明切换项；真源 B:317「快速模式：」（B.4.1/INT-D02 允许答案族分两族）
hard_rule_refs:                            # B.4.1/INT-D02（B:305-347）无"冻结事实"块、未列任何规则/禁令对象；仓库中真实落盘的规则对象目前仅 BD-D01 fixtures 的 R-BDD01-001/002，本案例不得凭空断言 object_id/version。caveat：B:333 禁止结果点名「编造创始人背景、账号关系或品牌禁语」，IA-0 冻结的品牌事实若把禁语清单登记成硬规则对象，须在此回填 → OQ-INT-D02-02（08-17 已失效：contracts/rules/ 现已落盘 R-FB01-001 / R-BDD01-001 / R-BDD01-002 三条 RuleRecord）
  # 【08-17 回填】上行 caveat 的兑现条件已满足：Founder 预裁决③「品牌禁语立 RuleRecord」已落盘
  #   contracts/rules/R-FB01-001.yaml（词表唯一真源 = acceptance/detectors/forbidden_lexicon.yaml）。
  #   本案例登记依据：B:333 禁止结果明文点名「编造创始人背景、账号关系或品牌禁语」——品牌禁语为本案例明文涉及项。
  #   OQ-INT-D02-02 就此关闭。不登记 R-BDD01-001/002：该两条 source_ref 指向 BD-D01 案例夹具，跨案例引用会把 BD-D01 考点带入本案例。
  - {object_type: RuleRecord, object_id: R-FB01-001, version: 1, brand_id: fixture-brand-01}
output_schema_version: "v0.1"                 # 占位值：contracts/schemas/intent_execution_plan.schema.json 未声明版本号，B 也未规定诊断案例的输出合同版本 →（三案例同问）OQ-INT-D01-05
brand_memory_state: DISABLED                  # B:95「首轮验收关闭 Brand Memory。」
approved_brand_memory_refs: []                # B:179「DISABLED 时引用列表必须为空。」
e2e_interaction_contract_version: null        # B.2.3（B:186）的共同两阶段交互合同写在「端到端 A/B」名下；本案例属 B.4 模块诊断案例（B:257-263），B 未把该合同套用于诊断案例
e2e_output_contract_version: null             # 同上，真源 B.2.3（B:183-204）
baseline_prompt_versions:
  decision_stage: null                        # B.2.4（B:205-207）的基线两阶段调用同样写在「正式基线／端到端 A/B」名下；B 未要求 B.4 诊断案例跑基线侧
  creative_stage: null
diyu_build_version: "PENDING_BUILD"           # 笛语侧尚未构建，无可引用的构建版本
module_contract_versions:
  intent: "PENDING_BUILD"
  business_decision: "PENDING_BUILD"
  creative: "PENDING_BUILD"
model_provider: "Alibaba Cloud DashScope"  # Founder 2026-08-17 IA-0 定格（实测两家 models 接口后裁决；参数真源 contracts/interaction/generation_parameters.json）
model_name: "qwen-max-0107"  # Founder 2026-08-17 IA-0 定格（实测两家 models 接口后裁决；参数真源 contracts/interaction/generation_parameters.json）
model_version: "snapshot-0107（models 接口回执 2026-08-17）"  # Founder 2026-08-17 IA-0 定格（实测两家 models 接口后裁决；参数真源 contracts/interaction/generation_parameters.json）
generation_parameters_hash: "sha256:24b85c93d027274afddbb347faf50533822566aa2b0f14365f4a62c4e2759262"  # Founder 2026-08-17 IA-0 定格（实测两家 models 接口后裁决；参数真源 contracts/interaction/generation_parameters.json）
allowed_tools: []                             # 占位保守值：不声明任何工具。B:178 只写「不允许任一侧使用未声明的搜索或知识库」（且 B.2.2 自述适用于正式 A/B），B 未规定诊断案例 allowed_tools 必须为空数组 →（三案例同问）OQ-INT-D01-04
approved_by: "Founder（Faye）"  # IA-0 签字（2026-08-17 AskUserQuestion 三连裁决，台账登记）
approved_at: "2026-08-17T19:08:38+08:00"
