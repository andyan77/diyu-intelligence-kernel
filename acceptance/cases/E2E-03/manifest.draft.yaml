# DRAFT——PENDING 字段于 IA-0 冻结时补齐并经 Founder 签字生效
# 案例真源：B.5.1/E2E-03（B:759-763，只引用不复述）
# 字段定义真源：B.2.1 Case Manifest（B:131-163）+ contracts/schemas/case_manifest.schema.json
# 运行份数：本案例为单次运行案例（B.5.1 未要求同案例多次运行），故只此一份运行 Manifest。
#
# 起草说明（不构成对 B 的复述，仅记录本草案取值依据）：
# - task_statement：B.5.1/E2E-03 无「输入」引文段，也无「能力问题」原句；
#   B:761 只声明商品形态与「品牌、受众、Persona 和商业目标在 Manifest 中冻结」，
#   即商业目标原文由本 Manifest 在 IA-0 冻结、B 未预先给定。执行侧不得自拟目标，
#   故填 PENDING 占位（见 OPEN_QUESTIONS OQ-E2E-03-01）。
# - execution_mode：B.5.1/E2E-03 未声明 execution_mode；本值为占位（无 B 依据），
#   IA-0 由 Founder 裁决（见 OPEN_QUESTIONS OQ-E2E-03-02）。
# - hard_rule_refs：B.5.1/E2E-03 未明文冻结任何规则或禁令（品牌本身列为「在 Manifest 中冻结」项），
#   故为空数组；仓库 contracts/rules/ 目前也无任何 RuleRecord 落盘对象，不得凭空铸造 object_id/version。
#   若 IA-0 冻结的品牌事实附带硬规则，须在此回填（见 OPEN_QUESTIONS OQ-E2E-03-03）。
# - e2e_interaction_contract_version / e2e_output_contract_version：本案例属 B.5 端到端匿名 A/B，
#   两字段本应指向 B.2.3（B:183-203）要求的两侧共用外部交互/输出合同；但 contracts/interaction/README.md:63
#   已登记该缺口——「尚无一份两侧共用的独立合同文件」，故取 PENDING_IA0，不得写成有版本号的存在。
#   该合同文件落盘并按 contracts/interaction/README.md:40-41 升 v1.0 / FROZEN 后方可回填
#   （见 OPEN_QUESTIONS OQ-E2E-03-04）。
# - output_schema_version：B 未给版本号，contracts/schemas/ 内各 Schema 文件也未声明版本号；
#   现值沿用同批草案口径、无真源背书，须于 IA-0 定格（见 OPEN_QUESTIONS OQ-E2E-03-05）。
# - context_snapshot_id：规划名；本案例目录下尚无 fixtures/，快照未构建、hash 无法计算；
#   且 B:761「品牌、受众、Persona 和商业目标在 Manifest 中冻结」所指的品牌/受众/Persona 在
#   B.2.1 字段表（B:132-162）中无对应字段，落点待确认（见 OPEN_QUESTIONS OQ-E2E-03-06）。

case_id: E2E-03
case_version: "v0.1"                          # 与 acceptance/cases/BD-D01/case.yaml 的 case_version 口径一致；草案态由文件名 .draft.yaml + 本文件头 DRAFT 注释 + approved_by 表达，不进版本号
prd_version: "v0.1"                           # 版本基线见 M0-EP01_文档版本一致性声明.md 一、现行版本基线
data_contract_version: "v0.1"                 # 同上（附录 A v0.1）
acceptance_contract_version: "v0.3"           # 同上（附录 B v0.3）
context_snapshot_id: "SNAP-E2E03-0001"        # 规划名，快照尚未构建（OQ-E2E-03-06）
snapshot_hash: "PENDING_SNAPSHOT_BUILD"       # 快照构建后回填（B:139 字段为 string）
task_statement: "PENDING_IA0_TASK_STATEMENT——B.5.1/E2E-03 无「输入」引文亦无「能力问题」原句，且 B:761 声明商业目标由本 Manifest 冻结；待 Founder 于 IA-0 冻结下发给两侧的同一业务任务原文（OQ-E2E-03-01）。"
execution_mode: QUICK                         # 占位（无 B 依据），IA-0 由 Founder 裁决（OQ-E2E-03-02）
hard_rule_refs: []                            # B 未在本案例冻结规则对象；contracts/rules/ 亦无落盘对象（OQ-E2E-03-03）
output_schema_version: "v0.1"                 # 无真源背书，IA-0 定格（OQ-E2E-03-05）
brand_memory_state: "DISABLED"                # B.1.4（B:95）：首轮验收关闭 Brand Memory
approved_brand_memory_refs: []                # B.2.2（B:179）：DISABLED 时引用列表必须为空
e2e_interaction_contract_version: "PENDING_IA0"   # 共同交互合同文件尚不存在（contracts/interaction/README.md:63）
e2e_output_contract_version: "PENDING_IA0"        # 共同输出合同文件尚不存在（同上）
baseline_prompt_versions:
  decision_stage: "PENDING_IA0"               # 基线 Prompt 现为 v0.1-draft，draft 不得用于正式 A/B 取证（contracts/interaction/README.md:40）
  creative_stage: "PENDING_IA0"
diyu_build_version: "PENDING_BUILD"           # 笛语侧尚未构建
module_contract_versions:
  intent: "PENDING_BUILD"
  business_decision: "PENDING_BUILD"
  creative: "PENDING_BUILD"
# PENDING_OD02_IA0：OD-02 厂商组合已裁决（裁决台账 08-17），版本/参数于 IA-0 定格——占位指后者
model_provider: "PENDING_OD02_IA0"
model_name: "PENDING_OD02_IA0"
model_version: "PENDING_OD02_IA0"             # 同上；模型版本标识是否存在取决于 IA-0 定格的具体模型，未定即占位，不写 null（null = 断言「该模型无版本标识」）
generation_parameters_hash: "PENDING_OD02_IA0"
allowed_tools: []                             # 本案例未声明任何工具，故为空数组；B.2.2（B:178）禁止任一侧使用未声明的搜索或知识库
approved_by: "PENDING_FOUNDER_SIGNATURE_IA0"
approved_at: "PENDING_FOUNDER_SIGNATURE_IA0"  # B:162 该字段类型 datetime，签字时以真实时间戳回填
