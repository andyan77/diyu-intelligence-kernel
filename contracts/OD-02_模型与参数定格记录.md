# OD-02｜A/B 模型与参数定格记录

| 项 | 值 |
|---|---|
| 版本 | v1.1 |
| 状态 | EFFECTIVE（已生效；v1.0 由 Founder（Faye）重签 2026-08-18T02:39:10+08:00 生效；v1.1 主模型替换由 Founder 2026-08-18 选择题裁决生效，记录见《裁决台账》08-18「块 0 主模型裁决」行） |
| 定格事实 | Founder 2026-08-17 IA-0 三连裁决（模型定格 / 值类定格 / 确认签字）定格本文件全部取值；主模型于 2026-08-18 经实测确认不可调用后由 Founder 裁决替换（见 §四），其余取值不变 |
| 已裁决基础 | 台账 08-17：测试模型 = Qwen + DeepSeek 系列组合；多模态用 Qwen 系列；型号执行侧按最佳工程实践适配；正式版本/参数/基线 Prompt 于 IA-0 定格并签字 |
| 合同依据 | B.2.2（两侧同供应商同基础模型同版本同参数）；C.5 / C.10#1（trace_auditor 跨厂商：系统侧与审计侧分属两家） |
| 修订记录 | v0.1 提案起草（2026-08-17 M0-EP02 建设轮）→ v1.0 内容定稿（2026-08-17 M0 收口修复批次）→ v1.1 主模型 qwen-max-0107 实测不可调用，Founder 裁决替换为 qwen3-max-2026-01-23（2026-08-18，证据见 §四） |

## 一、角色分配（已定格）

| 角色 | 厂商/定格型号 | 定格依据 |
|---|---|---|
| **A/B 主模型**（笛语侧与基线侧同用） | **qwen3-max-2026-01-23**（Qwen 旗舰指令模型快照版；2026-08-18 真实调用回执，见 §四。替换前值 qwen-max-0107 已实测确认不可调用） | 中文商业文案与结构化输出能力强；两侧同模型满足 B.2.2"同条件"；快照版满足可复现要求 |
| **多模态**（商品图 VisualProfile） | qwen-vl-max（无快照版：锁别名，逐次运行留存接口返回的实际 model 回执） | OD-02 已裁决多模态用 Qwen 系；与主模型同厂减少集成面。"无快照版 → 锁别名 + 每次运行留存回执"是 Founder 已批准的例外处置 |
| **trace_auditor / L2 探针** | deepseek-v4-pro（跨厂商审计侧，非 A/B 参赛方） | 跨厂商审计：系统侧 Qwen、审计侧 DeepSeek，两家物理分离，防同门自审 |

> 溯源注：预裁决⑧原表述为 DeepSeek `deepseek-chat` 家族；IA-0 当日 models 接口实测后定格为 `deepseek-v4-pro`。取值以 `contracts/interaction/generation_parameters.json` 为唯一真源（台账 08-17 与 IA-0 冻结签字包 §一 同口径）。
> 核验待办（修复批次 P0-4）关闭记录：「按当前 HEAD 重新实测」已于 2026-08-18 执行（见 §四）——主模型实测不可调用，按 B.8.1 走变更即本 v1.1；多模态与审计侧型号本轮未再实测（外部审查 2026-08-18 报告 qwen-vl-max / deepseek-v4-pro 实调成功，正式运行前仍须逐次留回执）。
> **级联挂起（改考卷，本轮不动）**：`contracts/interaction/generation_parameters.json` 的 model_name / model_version 与 20 份 Case Manifest 的同名字段仍承载替换前值 qwen-max-0107，与本表 v1.1 的不一致为**已登记的过渡态**；须在 IA-1 送签 / 首次正式运行前按 B.2.1（升 case_version）+ B.8.1 程序级联更新并重算 generation_parameters_hash。触发点已登记《裁决台账》挂起表。在级联完成前，任何以 qwen-max-0107 为条件的正式运行均不得启动。

## 二、生成参数（已定格）

数值真源 = `contracts/interaction/generation_parameters.json`（该文件自述为 `generation_parameters_hash` 的唯一真源）。本表为其只读转录，冲突以 json 为准。

| 参数 | 定格值 | 说明 |
|---|---|---|
| temperature | 0.3 | 决策/结构化输出场景压随机性；创意阶段**不**单独放宽（json 为全局单值） |
| top_p | 0.8 | 配合低温 |
| seed | 20260817 | 可复跑性 |
| max_tokens | 阶段 D 4096 / 阶段 C 8192 | 防无限调用换质量（B.2.2） |
| 输出语言 | zh-CN | 两侧同外层展示口径（B.2.5 B:225） |
| 采样口径 | 日常 n=1；Gate 正式验收 n=3，任一次命中一票阻断标签即记（就严不就宽） | C.6 已裁决口径，照录 |

## 三、定格执行留痕（2026-08-17；防幻觉：版本串一律实测不手写）

1. ✅ 以 `.env` 密钥只读调用两家 models API，列出当日实际可用型号与版本串；
2. ✅ 实测版本串填入 **20 份** Case Manifest 的 `model_provider` / `model_name` / `model_version`；
3. ✅ 参数表定格 → 计算 `generation_parameters_hash` 并填入 20 份 Manifest（当前落盘值 `sha256:24b85c93d027274afddbb347faf50533822566aa2b0f14365f4a62c4e2759262`，20 份同值）；数值真源落 `contracts/interaction/generation_parameters.json`；
4. ✅ 与 20 份 Manifest、两阶段基线 Prompt、匿名流程同包提交 Founder 签字（B.2.1 `approved_by`），首签 2026-08-17T19:08:38+08:00 ×20 份（该次 M0 收口宣告已于同日撤回，重签见《M0收口修复批次_执行规格.md》P0-6）。

> 防幻觉纪律（保留）：本文件所有版本串一律来自 API 实测回执，禁止凭训练记忆手写。§一§二为 2026-08-17 实测定格值（主模型除外，见 §四）；任何后续版本变更须重新实测并升本文件版本。

## 四、主模型替换实测留痕（2026-08-18，v1.1 修订依据）

背景：三方审查（M0 审查三）指控主模型 qwen-max-0107 不可调用。执行侧按「先复现、后裁决」逐项实测（实测时间 2026-08-17T21:34:06-07:00，即北京时间 2026-08-18 午间；以 `.env` DASHSCOPE_API_KEY 只读调用）：

| # | 实测项 | 结果（HTTP 状态 + 关键回执） |
|---|---|---|
| 1 | models 接口（compatible-mode /v1/models） | HTTP 200；`qwen-max-0107` **仍在列**——在列 ≠ 可调用，这正是 IA-0 当日「models 接口回执」未能暴露问题的原因 |
| 2 | qwen-max-0107 @ 兼容端点 chat/completions | **HTTP 404** `model_not_found`（"The model `qwen-max-0107` does not exist or you do not have access to it."，request_id afb5f957-6156-9142-9cc9-52f48c398c00） |
| 3 | qwen-max-0107 @ 原生端点 text-generation | **HTTP 400** `InvalidParameter: Model not exist.`（request_id 031912df-9d54-96cc-89a3-d29bed21b63c） |
| 4 | 候选 qwen3-max-2026-01-23 @ 兼容端点真实调用 | **HTTP 200**；回执 model=`qwen3-max-2026-01-23`，实答「收到」（request_id chatcmpl-e5c5f74f-08…） |
| 5 | 候选 qwen3-max-2025-09-23 @ 兼容端点真实调用 | HTTP 200；回执 model=`qwen3-max-2025-09-23`，实答「收到」（request_id chatcmpl-046aa035-2e…） |
| 6 | 候选 qwen-max-1201 @ 兼容端点 | HTTP 429 限流 ×4 次（间隔重试）——型号在列但当前账号持续调不通，**不列为可用候选**（如实报告，不硬判死活） |

审查三指控判定：**属实**（#2/#3 与其报告的 404/400 逐字吻合）。Founder 裁决（2026-08-18 选择题）：主模型替换为 **qwen3-max-2026-01-23**（两个实测可用候选中最新快照，可用寿命预期最长；qwen-max-0107 之死即老快照下线的前车之鉴）。审计侧 deepseek-v4-pro、多模态 qwen-vl-max 不在本次替换范围。
