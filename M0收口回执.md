# M0 收口回执（M0 Closeout Receipt）

| 项 | 值 |
|---|---|
| 受审树 / audited tree | commit `24e24a39288bf8965e834da9ba048b6d30708093`（origin/main） |
| 取证方式 | **干净 clone**：全新空目录 `git clone` 自 GitHub 远程 → `python3 -m venv` → `pip install -r requirements.txt`（钉版）→ `bash tools/install_hooks.sh` → `python tools/check_m0.py`，全程一键、零手工干预 |
| 环境 | Python 3.10.12；jsonschema==3.2.0；PyYAML==6.0.2（pip freeze 实读） |
| 取证时间 | 2026-08-17（会话内后台任务 bgg5zjt46，输出逐字存档于会话 repair-batch 目录） |
| 生成本回执时 HEAD 将前进一格 | 回执描述的是 `24e24a3` 的树内容；回执自身入库产生后继提交，属正常取证模式 |

## 一、check_m0 五步实测（干净 clone 内原样输出）

```text
[1/5] PASS 冻结断言门 送签态 | GATE_GREEN、exit 0（十三条红线全清）
[2/5] PASS 冻结门负向变异回归 | 22 项全过、真仓零改动隔离 PASS、exit 0
[3/5] PASS 检测器负向测试 | 合计 80 项：通过 80、失败 0、exit 0
[4/5] PASS BD-D01 三夹具回归 | good=PENDING_HUMAN / collapse=FAIL[BD_CANDIDATE_COLLAPSE] / fabricated=FAIL[BD_FACT_FABRICATION]（PENDING_HUMAN ≠ 通过，PASS 只属 L3 人工）
[5/5] PASS Schema 四例烟测 | 4/4 如期（2 正 + 1 结构违约 + 1 不可核验；结构过 ≠ 内容正确）
CHECK_M0_GREEN | 五步全绿 | HEAD=24e24a39288bf8965e834da9ba048b6d30708093
```

运行态红线（设计内，不计判绿）：110 条 = R2×80（PENDING_BUILD 四构建字段，M1 填）+ R11×10（PENDING_RESIGN 占位）+ R12×20（撤回签字待重签）。**重签后 R11/R12 归零，基线回落 80。**

## 二、服务端 CI（GitHub Actions，required job = check_m0）

| commit | 结论 | 运行 |
|---|---|---|
| `20758eb`（CI 首跑） | ✅ success | actions/runs/32055027654 |
| `24e24a3`（受审树） | ✅ success | actions/runs/32055498550 |

## 三、外部审查六项阻断 → 关闭对照

| 阻断 | 关闭方式 | 关闭证据 |
|---|---|---|
| BLOCK-01 冻结靠首行覆盖 | P0-1：176 处残留逐处关闭、P-01~14 全回填、Manifest 去 draft、签字包/OD-02 单义化；生效戳一律 PENDING_RESIGN 占位（重签前不写 EFFECTIVE） | `cda86cc` + 三轮对抗核验 26 条全处置 |
| BLOCK-02 冻结门假绿 | P0-2：R8-R13 六红线 + 变异回归 22 项固化（考官全部攻击手法转为永久红测）+ 渗透审查 7 blocker 补堵 | `a548be5`；本回执 [2/5] |
| BLOCK-03 数字漏检+证据过期 | P0-3 检测器 v0.3（单位分类绑定/中文数字/禁借位）+ P0-4 三份 RUN 按 HEAD 重跑（provenance 带 SHA/环境/词表16） | `a548be5` `3318d06`；本回执 [3/5][4/5] |
| BLOCK-04 OD-05 未裁决 | Founder 08-17 裁决：轻量自建 | `04b5adb` |
| BLOCK-05 无可传播门禁 | P0-5：requirements 钉版（实测 PyYAML 重钉）+ check_m0 统一入口 + CI 服务端双绿 + 钩子安装脚本；干净 clone 一键复现即本回执本体。分支保护（required check=check_m0）于重签推送后启用 | `3318d06` `20758eb`；本回执全文 |
| BLOCK-06 云盘边界 | Founder 08-17 特别说明裁决：不阻塞、不列确认项，文件 Founder 自行删除（工作树脱敏 72be502 已完成） | 台账 08-17 行 |

## 四、签字区

| 动作 | 签字人 | 时间 | 状态 |
|---|---|---|---|
| IA-0 首签 | Founder（Faye） | 2026-08-17T19:08:38+08:00 | 已撤回（外部审查 BLOCK，台账 08-17） |
| **IA-0 重签** | PENDING_RESIGN_P0-6 | PENDING_RESIGN_P0-6 | 待 Founder 基于本回执签署；签署后本表更新、20 份 Manifest 回填新时间、全仓占位清零、运行态红线回落 80 |
