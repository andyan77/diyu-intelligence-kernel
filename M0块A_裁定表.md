# M0 证据链加固（块 A）· 指控裁定表

> 性质：块 A（M0 证据链加固）交付物之一。依据 Founder 派发的三份 M0 三方审查报告（审查一判「成立」、审查二判「FAIL」、审查三判「未收口」），全部指控逐条编号、逐条**复现**后裁定。复现基线：main@37b2d55（块 0 合并后），独立 worktree，全部 runtime_verified（标注 static_verified 者除外）。
> 修复范围按派发包封顶六件（A-2.a~f）；范围外属实项**只登记不处理**（挂起表带触发点）。

## 一、裁定汇总

| # | 指控（来源） | 复现方法与结果 | 裁定 | 处置 |
|---|---|---|---|---|
| Z-01 | 收口回执只绑 `24e24a3`，重签发生在改动 37 文件的 `6525ca0`，最终签字树无绑定自身的干净 clone 回执（审查二 P0-01 / 审查三 P0-3 / 审查一 P3-4） | 读《M0收口回执.md》L5 绑定 SHA；`git diff --stat 24e24a3 6525ca0` = 37 文件（含 20 Manifest、冻结合同、门代码） | **属实** | A-3：块 A 合并后干净 clone 六步全绿，出具绑定最终 SHA 的新版回执，Founder 确认签字；永久规矩「先出回执、后签字」入执行规格 §四 |
| Z-02 | Manifest 冻结不充分：签字与资产内容零绑定，「改考题保签字」门仍绿；model 三字段不与参数真源交叉核验（审查二 P0-02 / 审查三 P1-1） | 变异实测：M23（改 task_statement 保签字）修复前 GATE_GREEN；M25（model_name 漂移）修复前 GATE_GREEN——两条假绿路径均复现 | **属实** | A-2.c：R6 内容绑定（`case_manifests` 节，去签字三字段的内容指纹实时比对）；A-2.d：R8 增查 model_provider/model_name/model_version 与 generation_parameters.json 逐字段全等。M23/M25 修复后转红（活体证据） |
| Z-03 | R5 只看键不看值，PROHIBIT 可改任意值；rules/*.yaml 不在 digest 冻结面，硬规则可不升版漂移（审查二 P0-03） | 源码自注「机械校验，不看值只看键」；frozen_digests `_scope` 无 rules；变异 M21（effect→FORBID）/M22（statement 改字不升版）修复前均 GATE_GREEN | **属实** | A-2.b：R5 值校验（scope/effect/status 枚举 + version/effective_at/statement 等类型逐项，A.9.1 逐字）+ `rules` 节 digest 冻结双向覆盖。M21/M22 修复后转红 |
| Z-04 | 重签提交替换七份冻结件 digest 而未追加 version_history，同版本双 digest、旧值只留 git 历史——违反登记册自身 append-only 规则（审查二 P0-04 / 审查三 P1-2） | 比对 `24e24a3` 与当前 frozen_digests.json：e2e_interaction_contract v1.0 旧 `sha256:7e49…` 被 `5cdc…` 覆盖，history 仅 1 条新值；七份同样 | **属实** | A-2.a（二选一取「补历史+例外条款」，台账留痕）：七份旧条目按历史事实补回 history 首位，重签条目加 `supersession_kind: signature_state_flip` 显式标记，链全量重算；登记册 `_maintenance` 补例外条款严格边界；M26 变异证明无标记双 digest 仍被 R10 拦 |
| Z-05 | R5/R6/R7 三条红线无负向活体证据（审查一 P1-1 / 审查二 P1-01） | 变异套件自我披露原文 + grep 覆盖统计确认 | **属实** | A-2.e：M21/M22（R5）、M23（R6）、M24（R7）、M25（R8 身份）、M26（R10 翻转边界）六条进套件，22→28 项，先红后绿全程留痕 |
| Z-06 | C.2 隔离门只在本地钩子，服务端不强制；CI 依赖非完全封闭（审查二 P1-02 / P2-03 / 审查三 P1-3 后半） | 读 CI 文件自述（static_verified，CI 文件明文承认） | **属实** | 派发包写死块 A 不做——已登记台账挂起表，触发点 = IA-1 送签前 |
| Z-07 | RUN-0001/2/3 与 coverage 的 provenance 指向 `a548be5`，非收口树（审查二 P1-03 / P2-02 / 审查三 P2-2） | 读 RUN-0001 provenance.commit_sha = `a548be5f…`；coverage 同 | **属实（部分修）** | coverage 本批重生成（provenance 绑当前树，A-2.f）；三份 RUN 重跑不在六件封顶内→登记挂起，触发点 = 新版回执出具前（回执须核验 RUN 一致性口径） |
| Z-08 | M0-EP01 版本一致性声明漂移：E 仍登记 v1.0（实为 v1.1），文末保留已被重签取代的「收口已撤回」过期状态段（审查二 P2-01） | 读声明 L20 = v1.0、L40 过期段在 | **属实** | A-2.f：声明升 v1.1——E 刷新 v1.1、OD-02 指针刷新 v1.1、过期状态段删除 |
| Z-09 | 主模型 qwen-max-0107 不可调用（审查三 P0-1） | 两端点实调 404/400，逐字复现 | **属实** | ✅ 已在块 0 闭合（OD-02 v1.1，Founder 裁决替换 qwen3-max-2026-01-23；级联挂起） |
| Z-10 | OQ-BUILD-05/09/10/11 四项未关闭且未入台账挂起雷达（审查三 P0-2 / 审查一 P2-1） | 块 0 已复现处理 | **属实** | ✅ 已在块 0 闭合（09/10/11 裁决关闭、05 移挂起表） |
| Z-11 | CR-D02 夹具活占位 `PENDING_REVIEW_RECORD_IA0` 处于 R2/R11 双盲区（审查一 P1-2） | grep 命中 + 门绿共存（复现自审查一，本轮 grep 确认占位仍在） | **属实** | 占位本体随 OQ-BUILD-05 挂起（IA-3 / CR-D02 启用前裁决后落盘 ReviewRecord 即消）；扫描盲区不单独修——为一个已挂起占位扩门扫描面属过度工程化，裁定表留痕即雷达 |
| Z-12 | 变异套件运行态覆盖仅 3 条，其余只在送签态取证（审查一 P2-2） | 套件自我披露 | **属实（本轮不扩）** | 范围外登记：新增 M21-M26 亦为送签态；运行态逐条覆盖挂起，触发点 = M1 首次真跑前 |
| Z-13 | INT-D01 `_fixture_note`「待确认」列表含已裁决项，注记过期自相矛盾（审查一 P3-1） | 审查一已实测；夹具属考卷 | **属实（不修）** | 改夹具=改考卷，块 A 无此授权；OQ-BUILD-11 已裁决下发前剥离全部 `_` 前缀元数据——过期注记不会到达被测模块，风险已被裁决兜住；登记不处理 |
| Z-14 | 裸 PENDING 字样 4 处（合规范围）；sync_digest_registry 可显式重写历史；R11 跳过二进制（审查一 P3-2/P3-3） | 审查一自述+工具自述 | **属实（观察项）** | 均为已自述的诚实边界，不构成缺口，不修 |
| Z-15 | 收口推送时分支保护尚未启用，不能追溯证明 `6525ca0` 受保护（审查三 P1-3 前半） | 回执 L39 自述（static_verified） | **属实（不可逆）** | 历史事实无法修改；当前 main 已启用 required check + strict + enforce_admins（块 0/块 A PR 均实测走保护流程），后续提交全部受护 |
| Z-16 | 本地 HEAD 未推送、工作区有未提交改动（审查三 P2-1） | 派发包明示 EP02 窗口在途施工，其车道 = kernel/ + INT 考卷 | **不属实（按派发口径）** | EP02 在途分支属派发豁免，非 M0 证据链缺口；附证据：脏文件清单与 EP02 车道完全重合 |
| Z-17 | 「当前 HEAD 未取得本次独立运行结果，判 UNVERIFIED」（审查二末段） | 该审查环境无法 clone，自述口径限制 | **不属实（作为缺陷）** | 系审查环境限制而非仓库缺陷；本轮块 A worktree + CI 已在当前树取得六步全绿 runtime_verified，缺口不存在 |

## 二、修复后活体证据（本批新增，28/28 全过）

| 变异 | 撬的锁 | 修复前 | 修复后 |
|---|---|---|---|
| M21 effect→FORBID | R5 值校验 | GREEN（假绿） | **RED R5** |
| M22 statement 改字不升版 | R5 规则冻结 | GREEN（假绿） | **RED R5** |
| M23 改考题保签字 | R6 内容绑定 | GREEN（假绿） | **RED R6** |
| M24 B 指针 v0.4→v0.3 | R7 版本指针 | RED（实现在位，无活体证据） | **RED R7**（证据补齐） |
| M25 model_name 漂移 | R8 身份全等 | GREEN（假绿） | **RED R8** |
| M26 无标记同版本双 digest | R10 翻转豁免边界 | RED（R10-已有） | **RED R10**（边界证据补齐） |

R1-R13 十三条红线自此**全部**持有负向活体证据（R5/R6/R7 三把「没试过撬的锁」撬完）。
