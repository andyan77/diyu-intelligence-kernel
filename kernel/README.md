# kernel/ ｜考生区（唯一允许高频迭代的区域）

改 prompt/编排前必须在 acceptance/attempts/ 登记尝试条目（C.6.1：没有失败 run 不许改）。不得与 acceptance/ 或 contracts/ 同 commit（C.2 隔离门物理拦截）。每模块七类资产见 C.3——红线测试：删掉代码只留 prompt 行为不变 = 已退化为 Prompt 套壳。
